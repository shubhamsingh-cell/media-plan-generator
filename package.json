{
  "name": "media-plan-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for auto-format hooks (Python server, not a Node app)",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.py": [
      "python3 -m black --quiet"
    ],
    "*.{html,css,js}": [
      "prettier --write"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shubhamsingh-cell/media-plan-generator.git"
  },
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
